//Criar uma função que recebe um número como parâmetro e retorna o dobro desse número.

let numero = parseFloat(prompt('Digite um número'));

function dobro() {
    return (`O dobro é: ${numero = numero + numero}`);
}

alert(dobro(numero));